<% if @game.errors.any? %>
  <div>
    <% @game.errors.full_messages.each do |msg| %>
      <div class="error_explanation"><%= msg %></div>
    <% end %>
  </div>
<% end %>



<div class="reveal-modal medium myModal3" data-reveal>
  <h2>Log Your Game</h2>
      <div class="player_selection">
        <div class="row">
          <%= form_for @game do |f| %>
            <%= f.collection_select :player_id, Player.all, :id, :name, prompt: "Choose Player 1" %>
            <%= f.text_field :opponent_score %>
            <%= f.collection_select :opponent_id, Player.all, :id, :name, prompt: "Choose Player 2" %>
            <%= f.text_field :player_score %>
            <button class="log-a-game-button">Submit</button>
            <% end %>
          </div>
  <a class="close-reveal-modal">&#215;</a>
        </div>
      </div>
</div>

<!-- 
<%=# form_for @game do |f| %>
 <div class="form-wrapper">
   <div class="log-game-title">Log a Game</div>
   <span><a href="#close" id="close">&#215;</a>
  <div class="choose-player-one"><%=# f.collection_select :player_id, Player.all, :id, :name, prompt: "Choose Player 1" %></div>

  <div class="choose-player-two"><%=# f.collection_select :opponent_id, Player.all, :id, :name, prompt: "Choose Player 2" %></div>

  <div class="player-one-score-label"><%=# f.label :player_one_score %></div> 
  <div class="player-one-score"><%=# f.text_field :player_score %></div><br />

  <div class="player-two-score-label"><%=# f.label :player_two_score %></div>
  <div class="player-one-score"><%=# f.text_field :opponent_score %><br /></div>

  <div class="log-game-submit"><%= f.submit %></div>

<%# end %>
</div>
 -->

<!-- <script class="cssdeck" src="//cdnjs.cloudflare.com/ajax/libs/jquery/1.8.0/jquery.min.js"></script> -->
